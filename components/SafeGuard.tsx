import React from 'react';
import { AgentSafeguardView } from './AgentSafeguardView';

/**
 * SafeGuard component providing the complete protective architecture:
 * - Pre-net (Staging Shield)
 * - Concern (Cognitive & Anomaly Audit)
 * - Worry (Sandbox Containment)
 * - Prestigious Moment (Cryptographic Consensus & Verification)
 * - Circular Whirpendale (Continuous Rotary Deflection)
 */
export const SafeGuard: React.FC = (props) => {
  return <AgentSafeguardView {...props} />;
};

export default SafeGuard;
