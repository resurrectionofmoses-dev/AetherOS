import React, { useState, useEffect, useRef, useMemo } from 'react';
import { motion, AnimatePresence } from 'motion/react';
import { 
    CodeIcon, SignalIcon, ZapIcon, ActivityIcon, ShieldIcon, 
    TerminalIcon, LogicIcon, SearchIcon, FireIcon, GaugeIcon, SpinnerIcon,
    BuildIcon, PackageIcon, CheckCircleIcon, WarningIcon, ServerIcon,
    ChevronDownIcon, ChevronUpIcon, XIcon, FileIcon, SearchIcon as SearchIcon2
} from './icons';

interface ModuleStat {
    name: string;
    path: string;
    bytes: number;
    lines: number;
    chars: number;
    importsCount: number;
    hooksCount: number;
    functionsCount: number;
    imports: string[];
    category: 'Component Module' | 'System Core';
    useStateCount?: number;
    useEffectCount?: number;
    allImports?: string[];
    maintainabilityIndex?: number;
}

const INITIAL_R_JAVA = `/* AUTO-GENERATED FILE. DO NOT MODIFY.
 *
 * This class was automatically generated by the
 * aapt tool from the resource data it found. It
 * should not be modified by hand.
 */

package github.com.Rkwideman42;

public final class R {
  public static final class anim {
    public static final int abc_fade_in=0x7f010000;
    public static final int abc_fade_out=0x7f010001;
    public static final int abc_grow_fade_in_from_bottom=0x7f010002;
    public static final int abc_popup_enter=0x7f010003;
    public static final int abc_popup_exit=0x7f010004;
    public static final int abc_shrink_fade_out_from_bottom=0x7f010005;
    public static final int abc_slide_in_bottom=0x7f010006;
    public static final int abc_slide_in_top=0x7f010007;
    public static final int abc_slide_out_bottom=0x7f010008;
    public static final int abc_slide_out_top=0x7f010009;
    public static final int abc_tooltip_enter=0x7f01000a;
    public static final int abc_tooltip_exit=0x7f01000b;
    public static final int btn_checkbox_to_checked_box_inner_merged_animation=0x7f01000c;
    public static final int btn_checkbox_to_checked_box_outer_merged_animation=0x7f01000d;
    public static final int btn_checkbox_to_checked_icon_null_animation=0x7f01000e;
    public static final int btn_checkbox_to_unchecked_box_inner_merged_animation=0x7f01000f;
    public static final int btn_checkbox_to_unchecked_check_path_merged_animation=0x7f010010;
    public static final int btn_checkbox_to_unchecked_icon_null_animation=0x7f010011;
    public static final int btn_radio_to_off_mtrl_dot_group_animation=0x7f010012;
    public static final int btn_radio_to_off_mtrl_ring_outer_animation=0x7f010013;
    public static final int btn_radio_to_off_mtrl_ring_outer_path_animation=0x7f010014;
    public static final int btn_radio_to_on_mtrl_dot_group_animation=0x7f010015;
    public static final int btn_radio_to_on_mtrl_ring_outer_animation=0x7f010016;
    public static final int btn_radio_to_on_mtrl_ring_outer_path_animation=0x7f010017;
    public static final int design_bottom_sheet_slide_in=0x7f010018;
    public static final int design_bottom_sheet_slide_out=0x7f010019;
    public static final int design_snackbar_in=0x7f01001a;
    public static final int design_snackbar_out=0x7f01001b;
    public static final int fragment_fast_out_extra_slow_in=0x7f01001c;
    public static final int mtrl_bottom_sheet_slide_in=0x7f01001d;
    public static final int mtrl_bottom_sheet_slide_out=0x7f01001e;
    public static final int mtrl_card_lowers_interpolator=0x7f01001f;
  }
  public static final class animator {
    public static final int design_appbar_state_list_animator=0x7f020000;
    public static final int design_fab_hide_motion_spec=0x7f020001;
    public static final int design_fab_show_motion_spec=0x7f020002;
    public static final int fragment_close_enter=0x7f020003;
    public static final int fragment_close_exit=0x7f020004;
    public static final int fragment_fade_enter=0x7f020005;
    public static final int fragment_fade_exit=0x7f020006;
    public static final int fragment_open_enter=0x7f020007;
    public static final int fragment_open_exit=0x7f020008;
    public static final int linear_indeterminate_line1_head_interpolator=0x7f020009;
    public static final int linear_indeterminate_line1_tail_interpolator=0x7f02000a;
    public static final int linear_indeterminate_line2_head_interpolator=0x7f02000b;
    public static final int linear_indeterminate_line2_tail_interpolator=0x7f02000c;
    public static final int mtrl_btn_state_list_anim=0x7f02000d;
    public static final int mtrl_btn_unelevated_state_list_anim=0x7f02000e;
    public static final int mtrl_card_state_list_anim=0x7f02000f;
    public static final int mtrl_chip_state_list_anim=0x7f020010;
    public static final int mtrl_extended_fab_change_size_collapse_motion_spec=0x7f020011;
    public static final int mtrl_extended_fab_change_size_expand_motion_spec=0x7f020012;
    public static final int mtrl_extended_fab_hide_motion_spec=0x7f020013;
    public static final int mtrl_extended_fab_show_motion_spec=0x7f020014;
    public static final int mtrl_extended_fab_state_list_animator=0x7f020015;
    public static final int mtrl_fab_hide_motion_spec=0x7f020016;
    public static final int mtrl_fab_show_motion_spec=0x7f020017;
    public static final int mtrl_fab_transformation_sheet_collapse_spec=0x7f020018;
    public static final int mtrl_fab_transformation_sheet_expand_spec=0x7f020019;
  }
  public static final class attr {
    public static final int ConstraintRotate=0x7f030000;
    public static final int SharedValue=0x7f030001;
    public static final int SharedValueId=0x7f030002;
    public static final int actionBarDivider=0x7f030003;
    public static final int actionBarItemBackground=0x7f030004;
    public static final int actionBarPopupTheme=0x7f030005;
    public static final int actionBarSize=0x7f030006;
  }
}`;

const generateVirtualSplitFiles = (module: ModuleStat, content: string) => {
    const lines = content.split('\n');
    const baseName = module.name.replace(/\.[a-zA-Z]+$/, '');

    // Types Split
    const interfaces = lines.filter(l => l.trim().startsWith('interface') || l.trim().startsWith('type') || l.trim().startsWith('export interface') || l.trim().startsWith('export type'));
    const typesCode = `// FILE: ${baseName}Types.ts
// Extracted Types & Config parameters representing physical state boundaries

import React from 'react';

${interfaces.length > 0 ? interfaces.join('\n') : `export interface ${baseName}Props {
    className?: string;
    isActive?: boolean;
    onReset?: () => void;
}

export type HealthStatus = 'EXCELLENT' | 'STABLE' | 'HEAVY_REFACTOR';`}

export interface MetricCardConfig {
    label: string;
    metric: string | number;
    subtext: string;
    variant: 'optimal' | 'moderate' | 'warning';
}
`;

    // Logic Split
    const stateHookLines = lines.filter(l => l.includes('useState') || l.includes('useEffect') || l.includes('useRef'));
    const logicCode = `// FILE: use${baseName}Workspace.ts
// Custom Hook encapsulating memory state and event callback handlers

import { useState, useEffect, useRef } from 'react';

export const use${baseName}Workspace = () => {
    // Encapsulated hook metrics:
    const [stateActive, setStateActive] = useState<boolean>(true);
    const [auditLog, setAuditLog] = useState<string>('Initialization completed.');
    
    ${stateHookLines.length > 0 ? stateHookLines.slice(0, 10).map(l => l.trim()).join('\n    ') : `const [progress, setProgress] = useState(0);`}

    const triggerBurst = () => {
        setAuditLog('Willpower trigger pulsed successfully.');
        setProgress(100);
    };

    return {
        stateActive,
        setStateActive,
        auditLog,
        triggerBurst
    };
};
`;

    // View Split
    const viewCode = `// FILE: ${baseName}Layout.tsx
// Visual design components utilizing modularized state hooks and types

import React from 'react';
import { motion } from 'motion/react';
import { ${baseName}Props } from './${baseName}Types';
import { use${baseName}Workspace } from './use${baseName}Workspace';

export const ${baseName}Layout: React.FC<${baseName}Props> = ({ className, isActive }) => {
    const { stateActive, auditLog, triggerBurst } = use${baseName}Workspace();

    return (
        <div className={\`p-6 bg-black border border-zinc-900 rounded-xl \${className}\`}>
            <div className="flex justify-between items-center pb-4">
                <h3 className="text-xs font-black tracking-widest text-rose-500 uppercase">
                    \${isActive ? 'ACTIVE' : 'STANDBY'} \${className || ''} PANEL
                </h3>
                <span className="text-[8px] text-zinc-500">STATUS: ACTIVE</span>
            </div>
            
            <div className="p-3 bg-zinc-950 border border-zinc-900 rounded-lg text-[10px]">
                {auditLog}
            </div>

            <button 
                onClick={triggerBurst}
                className="w-full mt-4 py-2 bg-rose-600 hover:bg-rose-500 text-white font-bold rounded-lg transition-all"
            >
                IGNITE ACTION PORT
            </button>
        </div>
    );
};
`;

    return { typesCode, logicCode, viewCode };
};

export const CascadeInspectorView: React.FC = () => {
    const [modules, setModules] = useState<ModuleStat[]>([]);
    const [loading, setLoading] = useState(true);
    const [error, setError] = useState<string | null>(null);
    const [searchQuery, setSearchQuery] = useState('');
    const [selectedCategory, setSelectedCategory] = useState<'ALL' | 'Component Module' | 'System Core'>('ALL');
    const [sortBy, setSortBy] = useState<'lines' | 'bytes' | 'name'>('lines');
    const [selectedModule, setSelectedModule] = useState<ModuleStat | null>(null);
    const [moduleContent, setModuleContent] = useState<string>('');
    const [loadingContent, setLoadingContent] = useState(false);
    const [cascadeActive, setCascadeActive] = useState(true);
    const [cascadeGravity, setCascadeGravity] = useState(3.0);
    const [searchCodeQuery, setSearchCodeQuery] = useState('');

    // Tab view selection
    const [activeTab, setActiveTab] = useState<'MODULES' | 'AAPT_MAPPER'>('MODULES');
    const [detailsTab, setDetailsTab] = useState<'audit' | 'dependencies' | 'refactor' | 'source'>('audit');
    const [simulatedSplitPart, setSimulatedSplitPart] = useState<'types' | 'logic' | 'view'>('types');

    // Android/AAPT states
    const [rJavaInput, setRJavaInput] = useState(INITIAL_R_JAVA);
    const [aaptActive, setAaptActive] = useState(true);
    const [selectedAaptClass, setSelectedAaptClass] = useState<string>('all');
    const [selectedAaptRes, setSelectedAaptRes] = useState<{ className: string; name: string; hex: string } | null>(null);
    const [searchAaptQuery, setSearchAaptQuery] = useState('');
    const [outlinePadding, setOutlinePadding] = useState<number>(4);
    const [willPower, setWillPower] = useState<number>(60);
    const [rotationOutline, setRotationOutline] = useState<boolean>(true);
    const [shakingResonance, setResonanceShaking] = useState<boolean>(false);
    const [activeOutlineAnim, setActiveOutlineOutlineAnim] = useState<string | null>(null);
    const [outlineScale, setOutlineScale] = useState<number>(1.0);
    const [simulatedAaptLog, setSimulatedAaptLog] = useState<string>('[AAPT INGESTION OK] System prepared. Awaiting compile signal.');
    const [compareSourceId, setCompareSourceId] = useState<string>('');
    const [compareTargetId, setCompareTargetId] = useState<string>('');

    // Dynamic parsing of inputted R.java string using a memoized parser
    const aaptResources = useMemo(() => {
        const parsed: { className: string; name: string; hex: string }[] = [];
        const lines = rJavaInput.split('\n');
        let currentGroup = 'unknown';

        for (const line of lines) {
            const classMatch = line.match(/public\s+static\s+final\s+class\s+(\w+)\s*\{/);
            if (classMatch) {
                currentGroup = classMatch[1];
                continue;
            }

            const fieldMatch = line.match(/public\s+static\s+final\s+int\s+(\w+)\s*=\s*(0x[0-9a-fA-F]+);/);
            if (fieldMatch) {
                parsed.push({
                    className: currentGroup,
                    name: fieldMatch[1],
                    hex: fieldMatch[2]
                });
            }
        }
        return parsed;
    }, [rJavaInput]);

    // Unique classes list from parsed entries
    const aaptClasses = useMemo(() => {
        const classes = new Set<string>();
        aaptResources.forEach(res => classes.add(res.className));
        return Array.from(classes);
    }, [aaptResources]);

    // Filter resources based on query and group filter
    const filteredAaptResources = useMemo(() => {
        return aaptResources.filter(r => {
            const matchesClass = selectedAaptClass === 'all' || r.className === selectedAaptClass;
            const matchesQuery = r.name.toLowerCase().includes(searchAaptQuery.toLowerCase()) || 
                                 r.hex.toLowerCase().includes(searchAaptQuery.toLowerCase());
            return matchesClass && matchesQuery;
        });
    }, [aaptResources, selectedAaptClass, searchAaptQuery]);

    // Dynamic Cascade Difference Calculator: parses hex indices and returns distance offsets
    const addrDiffValue = useMemo(() => {
        if (!compareSourceId || !compareTargetId) return null;
        const resA = aaptResources.find(r => r.name === compareSourceId);
        const resB = aaptResources.find(r => r.name === compareTargetId);
        if (!resA || !resB) return null;

        const valA = parseInt(resA.hex, 16);
        const valB = parseInt(resB.hex, 16);
        const difference = Math.abs(valA - valB);

        return {
            sourceRes: resA,
            targetRes: resB,
            diffHex: '0x' + difference.toString(16).toUpperCase(),
            diffDecimal: difference,
            isSameClass: resA.className === resB.className,
            relation: difference === 0 
                ? 'IDENTICAL' 
                : difference === 1 
                    ? 'CONSECUTIVE POINT' 
                    : (difference < 16 ? 'IMMEDIATE CO-NEIGHBOR' : 'CASCADING JUMP NODE'),
        };
    }, [compareSourceId, compareTargetId, aaptResources]);

    // Preselect initial source & target elements for differences analysis
    useEffect(() => {
        if (aaptResources.length >= 2) {
            if (!compareSourceId) setCompareSourceId(aaptResources[0].name);
            if (!compareTargetId) setCompareTargetId(aaptResources[1].name);
        }
    }, [aaptResources, compareSourceId, compareTargetId]);

    // Internal Audio tone generator for operator resonance feedback
    const playTapSound = (freq = 800, type: OscillatorType = 'sine', duration = 0.06) => {
        try {
            const audioCtx = new (window.AudioContext || (window as any).webkitAudioContext)();
            const oscillator = audioCtx.createOscillator();
            const gainNode = audioCtx.createGain();

            oscillator.type = type;
            oscillator.frequency.setValueAtTime(freq, audioCtx.currentTime);

            gainNode.gain.setValueAtTime(0.04, audioCtx.currentTime);
            gainNode.gain.exponentialRampToValueAtTime(0.0001, audioCtx.currentTime + duration);

            oscillator.connect(gainNode);
            gainNode.connect(audioCtx.destination);

            oscillator.start();
            oscillator.stop(audioCtx.currentTime + duration);
        } catch (e) {
            // Silently swallow browser-level sandbox constraints or blockages
        }
    };

    const canvasRef = useRef<HTMLCanvasElement>(null);

    // Fetch repository module data from the custom inspection endpoint
    useEffect(() => {
        const fetchModules = async () => {
            try {
                setLoading(true);
                const res = await fetch('/api/module-inspector');
                const data = await res.json();
                if (data.success) {
                    setModules(data.modules);
                } else {
                    setError(data.error || 'Failed to inspect system hierarchy.');
                }
            } catch (err: any) {
                setError(err.message || 'Error executing diagnostics trace.');
            } finally {
                setLoading(false);
            }
        };

        fetchModules();
    }, []);

    // Load full code content when a module is selected for investigation
    useEffect(() => {
        if (!selectedModule) {
            setModuleContent('');
            return;
        }

        const fetchContent = async () => {
            try {
                setLoadingContent(true);
                const res = await fetch(`/api/module-content?path=${encodeURIComponent(selectedModule.path)}`);
                const data = await res.json();
                if (data.success) {
                    setModuleContent(data.content);
                } else {
                    setModuleContent(`// Error loading file contents: ${data.error}`);
                }
            } catch (err: any) {
                setModuleContent(`// Traversal error: ${err.message}`);
            } finally {
                setLoadingContent(false);
            }
        };

        fetchContent();
    }, [selectedModule]);

    // Derived stats metrics
    const totals = useMemo(() => {
        return modules.reduce((acc, m) => {
            acc.lines += m.lines;
            acc.bytes += m.bytes;
            acc.chars += m.chars;
            acc.functions += m.functionsCount;
            return acc;
        }, { lines: 0, bytes: 0, chars: 0, functions: 0 });
    }, [modules]);

    const averageSize = useMemo(() => {
        if (modules.length === 0) return 0;
        return Math.round(totals.lines / modules.length);
    }, [modules, totals]);

    // Filtered & sorted list
    const filteredModules = useMemo(() => {
        let result = modules.filter(m => {
            const matchesSearch = m.name.toLowerCase().includes(searchQuery.toLowerCase()) || 
                                m.path.toLowerCase().includes(searchQuery.toLowerCase());
            const matchesCategory = selectedCategory === 'ALL' || m.category === selectedCategory;
            return matchesSearch && matchesCategory;
        });

        result.sort((a, b) => {
            if (sortBy === 'lines') return b.lines - a.lines;
            if (sortBy === 'bytes') return b.bytes - a.bytes;
            return a.name.localeCompare(b.name);
        });

        return result;
    }, [modules, searchQuery, selectedCategory, sortBy]);

    // Canvas matrix cascade fall speed proportionate to module size
    useEffect(() => {
        if (!cascadeActive) return;
        const canvas = canvasRef.current;
        if (!canvas) return;
        const ctx = canvas.getContext('2d');
        if (!ctx) return;

        let animationFrame: number;
        
        // Handle resizing dynamically
        const resizeObserver = new ResizeObserver(() => {
            if (canvas.parentElement) {
                canvas.width = canvas.parentElement.clientWidth;
                canvas.height = canvas.parentElement.clientHeight || 250;
            }
        });
        resizeObserver.observe(canvas.parentElement || canvas);

        canvas.width = canvas.parentElement?.clientWidth || 600;
        canvas.height = canvas.parentElement?.clientHeight || 250;

        const fontSize = 10;
        const columns = Math.floor(canvas.width / 14);
        const drops = Array(columns).fill(0).map(() => Math.random() * -150);
        
        // Define keywords based on active view state (Standard vs AAPT mapper)
        const systemKeywords = [
            'AetherOS', '0x03E2_SYNC', 'App.tsx', 'ViewRegistry', 'types.ts',
            'SovereignShield', 'conjunction_gates', 'absolute_reliability',
            'fs.readFileSync', 'lines.split', 'importsCount', 'functionsCount',
            ...modules.map(m => m.name.replace('.tsx', ''))
        ];

        const aaptSymbols = aaptResources.map(r => `${r.name}=${r.hex}`);
        const activeKeywords = (activeTab === 'AAPT_MAPPER' && aaptActive && aaptSymbols.length > 0)
            ? [...aaptSymbols, 'AAPT_RESOURCE_OUTLINE', 'R.final', 'will_power', 'github', 'Rkwideman42']
            : systemKeywords;

        const charSets = Array(columns).fill(0).map(() => {
            const index = Math.floor(Math.random() * activeKeywords.length);
            return activeKeywords[index] || '01';
        });

        const draw = () => {
            ctx.fillStyle = 'rgba(3, 3, 5, 0.15)';
            ctx.fillRect(0, 0, canvas.width, canvas.height);

            ctx.font = `${fontSize}px monospace`;

            for (let i = 0; i < drops.length; i++) {
                const keyword = charSets[i];
                const char = keyword[Math.floor(drops[i] / fontSize) % keyword.length] || ' ';
                
                // Set color scheme dynamically: Rose/Crimson for normal modules, and Cyan/Amber for AAPT willpower
                const highlight = Math.random() > 0.98;
                if (activeTab === 'AAPT_MAPPER') {
                    const isFocusIntensity = willPower > 70;
                    ctx.fillStyle = highlight 
                        ? (isFocusIntensity ? '#f59e0b' : '#38bdf8') 
                        : (isFocusIntensity ? 'rgba(245, 158, 11, 0.45)' : 'rgba(56, 189, 248, 0.3)');
                } else {
                    ctx.fillStyle = highlight ? '#f43f5e' : 'rgba(239, 68, 68, 0.35)';
                }
                
                const x = i * 14;
                const y = drops[i];

                ctx.fillText(char, x, y);

                // Fall trigger
                if (y > canvas.height && Math.random() > 0.975) {
                    drops[i] = 0;
                    const index = Math.floor(Math.random() * activeKeywords.length);
                    charSets[i] = activeKeywords[index] || '0x03E2';
                }

                // Speed scale increases smoothly with willpower constant
                const willpowerMultiplier = 0.5 + (willPower / 50); // scales from 0.5x to 2.5x speed
                drops[i] += cascadeGravity * willpowerMultiplier * (0.8 + Math.random() * 0.4);
            }

            animationFrame = requestAnimationFrame(draw);
        };

        draw();

        return () => {
            cancelAnimationFrame(animationFrame);
            resizeObserver.disconnect();
        };
    }, [cascadeActive, cascadeGravity, modules, activeTab, aaptActive, aaptResources, willPower]);

    const formatBytes = (bytes: number) => {
        if (bytes < 1024) return `${bytes} B`;
        const kb = bytes / 1024;
        return `${kb.toFixed(1)} KB`;
    };

    return (
        <div id="aetheros-view-wrapper" className="flex-1 flex flex-col h-full bg-[#030305] text-zinc-250 p-6 overflow-y-auto font-mono text-xs selection:bg-rose-500/30">
            {/* Page Header */}
            <div className="flex flex-col md:flex-row justify-between items-start md:items-center border-b-2 border-rose-500/20 pb-4 mb-6">
                <div>
                    <div className="flex items-center gap-2">
                        <CodeIcon className="w-5 h-5 text-rose-500 animate-pulse" />
                        <h1 className="text-xl font-black text-rose-400 tracking-widest uppercase">MODULE SIZE CASCADE INVESTIGATOR</h1>
                    </div>
                    <p className="text-[10px] text-zinc-400 mt-1 font-bold">
                        DIAGNOSTIC LATENCY: <span className="text-emerald-400">0ms</span> // SCANNING FILESYSTEM COMPONENTS FOR DENSITY AND VOLUME
                    </p>
                </div>

                <div className="flex items-center gap-2 mt-4 md:mt-0">
                    <span className="text-[10px] text-zinc-500 uppercase font-bold">MATRIX CASCADE STREAMS:</span>
                    <button 
                        id="cascade-stutter-toggle-btn"
                        name="cascade_toggle"
                        onClick={() => setCascadeActive(!cascadeActive)}
                        className={`px-3 py-1.5 border-2 rounded-xl text-[10px] font-bold uppercase transition-all shadow-[0_0_8px_rgba(239,68,68,0.1)] ${cascadeActive ? 'bg-rose-950/40 border-rose-500 text-rose-400' : 'bg-black border-zinc-800 text-zinc-500'}`}
                    >
                        {cascadeActive ? '● STREAMS RUNNING' : '○ STREAMS PAUSED'}
                    </button>
                </div>
            </div>

            {/* Interactive Mode Tab Switcher */}
            <div className="flex border-b border-zinc-904 mb-6 gap-2">
                <button
                    id="cascade-mode-modules-tab"
                    onClick={() => {
                        setActiveTab('MODULES');
                        playTapSound(600, 'sine', 0.05);
                    }}
                    className={`pb-3 px-4 text-xs font-black uppercase tracking-widest relative transition-all cursor-pointer ${activeTab === 'MODULES' ? 'text-rose-400 font-bold' : 'text-zinc-500 hover:text-zinc-300'}`}
                >
                    [ FILE MODULE ANALYSIS ]
                    {activeTab === 'MODULES' && (
                        <motion.div layoutId="cascadeActiveBar" className="absolute bottom-0 left-0 right-0 h-0.5 bg-rose-500" />
                    )}
                </button>
                <button
                    id="cascade-mode-aapt-tab"
                    onClick={() => {
                        setActiveTab('AAPT_MAPPER');
                        playTapSound(880, 'triangle', 0.07);
                    }}
                    className={`pb-3 px-4 text-xs font-black uppercase tracking-widest relative transition-all cursor-pointer ${activeTab === 'AAPT_MAPPER' ? 'text-cyan-400 font-bold' : 'text-zinc-500 hover:text-zinc-300'}`}
                >
                    [ AAPT RESOURCE OUTLINER & WILLPOWER ]
                    {activeTab === 'AAPT_MAPPER' && (
                        <motion.div layoutId="cascadeActiveBar" className="absolute bottom-0 left-0 right-0 h-0.5 bg-cyan-500" />
                    )}
                </button>
            </div>

            {/* Error state */}
            {error && (
                <div className="mb-6 p-4 bg-rose-950/20 border-2 border-rose-500/30 text-rose-400 rounded-xl flex items-center gap-3">
                    <WarningIcon className="w-5 h-5 flex-shrink-0 animate-bounce" />
                    <div>
                        <div className="font-bold uppercase text-[10px]">DIAGNOSTIC FAULT DETECTED</div>
                        <div>{error}</div>
                    </div>
                </div>
            )}

            {/* Core Metrics Bento Grid */}
            {!loading && (
                <div className="grid grid-cols-2 md:grid-cols-5 gap-3 mb-6">
                    <div className="bg-black/80 border-2 border-zinc-900 rounded-xl p-4 flex flex-col justify-between">
                        <span className="text-[8px] text-zinc-500 font-bold uppercase tracking-wider">Scanned Modules</span>
                        <div className="text-xl font-black text-white mt-1">{modules.length}</div>
                        <span className="text-[8.5px] text-zinc-600 font-bold mt-2">Active Components</span>
                    </div>

                    <div className="bg-black/80 border-2 border-zinc-900 rounded-xl p-4 flex flex-col justify-between">
                        <span className="text-[8px] text-zinc-500 font-bold uppercase tracking-wider">Total Lines of Code</span>
                        <div className="text-xl font-black text-rose-400 mt-1">{totals.lines}</div>
                        <span className="text-[8.5px] text-zinc-600 font-bold mt-2">Physical Lines</span>
                    </div>

                    <div className="bg-black/80 border-2 border-zinc-900 rounded-xl p-4 flex flex-col justify-between">
                        <span className="text-[8px] text-zinc-500 font-bold uppercase tracking-wider">Codebase Payload</span>
                        <div className="text-xl font-black text-amber-500 mt-1">{formatBytes(totals.bytes)}</div>
                        <span className="text-[8.5px] text-zinc-600 font-bold mt-2">Cumulative Bytes</span>
                    </div>

                    <div className="bg-black/80 border-2 border-zinc-900 rounded-xl p-4 flex flex-col justify-between">
                        <span className="text-[8px] text-zinc-500 font-bold uppercase tracking-wider">Total Functions</span>
                        <div className="text-xl font-black text-cyan-400 mt-1">{totals.functions}</div>
                        <span className="text-[8.5px] text-zinc-600 font-bold mt-2">Discovered Blocks</span>
                    </div>

                    <div className="bg-black/80 border-2 border-zinc-900 rounded-xl p-4 col-span-2 md:col-span-1 flex flex-col justify-between">
                        <span className="text-[8px] text-zinc-500 font-bold uppercase tracking-wider">Avg Module Size</span>
                        <div className="text-xl font-black text-emerald-400 mt-1">{averageSize} lines</div>
                        <span className="text-[8.5px] text-zinc-605 font-bold mt-2">Density Balance</span>
                    </div>
                </div>
            )}

            {/* Cascade Graphic Waterfall Section */}
            {cascadeActive && (
                <div className="relative h-28 bg-[#040406] border-2 border-zinc-900 rounded-xl overflow-hidden mb-6 flex flex-col justify-between">
                    <canvas ref={canvasRef} className="absolute inset-0 pointer-events-none opacity-40" />
                    
                    <div className="relative z-10 p-3 bg-gradient-to-b from-[#040406]/90 to-transparent flex justify-between items-center border-b border-zinc-900">
                        <div className="flex items-center gap-2">
                            <ActivityIcon className="w-3.5 h-3.5 text-rose-500 animate-pulse" />
                            <span className="text-[9px] font-black tracking-widest text-[#ef4444]">RECURSIVE MEMORY CASCADE SIMULATION</span>
                        </div>
                        <div className="flex items-center gap-2 bg-black/60 px-2 py-1 rounded border border-zinc-950">
                            <span className="text-[8px] text-zinc-500 font-bold uppercase">GRAVITY CONSTANT:</span>
                            <input 
                                id="cascade-gravity-range-input"
                                name="cascade_gravity"
                                type="range" 
                                min="1" 
                                max="8" 
                                step="0.5"
                                value={cascadeGravity}
                                onChange={(e) => setCascadeGravity(parseFloat(e.target.value))}
                                className="w-16 accent-rose-500 h-1"
                            />
                            <span className="text-[8px] text-rose-400 font-bold w-4 text-center">{cascadeGravity}</span>
                        </div>
                    </div>

                    <div className="relative z-10 p-3 flex justify-between items-end">
                        <div className="text-[8px] text-zinc-500">
                            // CONST COLLISION FLUIDITY IN SYSTEM MODULES: <span className="text-rose-400">ACTIVE</span>
                        </div>
                        <div className="text-[8px] text-zinc-500">
                            WATERFALL DRIFT RATE: {(cascadeGravity * 1.5).toFixed(1)} lines/s
                        </div>
                    </div>
                </div>
            )}

            {/* Main Diagnostics Workspace: Lists & Deep Code Inspection */}
            {activeTab === 'MODULES' ? (
                <div className="grid grid-cols-1 lg:grid-cols-12 gap-6 items-start">
                    
                    {/* Left Side: Staggered Cascade List (File Module Scanner) */}
                    <div className="lg:col-span-5 flex flex-col space-y-4">
                        {/* Filter and Command header */}
                        <div className="bg-black/90 border-2 border-zinc-900 rounded-xl p-4 space-y-3 shadow-[0_4px_16px_rgba(0,0,0,0.5)]">
                            <div className="flex justify-between items-center border-b border-zinc-950 pb-2">
                                <span className="text-[9px] font-black text-rose-400 uppercase tracking-widest">Diagnostic Filter Controls</span>
                                <span className="text-[8px] text-zinc-500">{filteredModules.length} Modules Matched</span>
                            </div>

                            {/* Search Bar */}
                            <div className="relative">
                                <input 
                                    id="cascade-module-search-input"
                                    name="cascade_query"
                                    type="text"
                                    placeholder="Query module name (e.g. App)..."
                                    value={searchQuery}
                                    onChange={(e) => setSearchQuery(e.target.value)}
                                    className="w-full bg-[#050508] border border-zinc-800 rounded-xl px-3 py-2 pl-8 text-white focus:outline-none focus:border-rose-500 text-[10px]"
                                />
                                <SearchIcon className="w-3.5 h-3.5 text-zinc-500 absolute left-2.5 top-2.5" />
                            </div>

                            <div className="grid grid-cols-2 gap-2 text-[9px]">
                                {/* Sort selection */}
                                <div className="flex flex-col gap-1">
                                    <span className="text-zinc-500 font-bold">SORT METRIC</span>
                                    <div className="grid grid-cols-3 border border-zinc-850 rounded overflow-hidden">
                                        <button 
                                            id="cascade-sort-lines-btn"
                                            name="sort_by_lines"
                                            onClick={() => setSortBy('lines')} 
                                            className={`py-1 font-bold ${sortBy === 'lines' ? 'bg-rose-950/40 text-rose-400' : 'bg-black text-zinc-500'}`}
                                        >
                                            LINES
                                        </button>
                                        <button 
                                            id="cascade-sort-bytes-btn"
                                            name="sort_by_bytes"
                                            onClick={() => setSortBy('bytes')} 
                                            className={`py-1 font-bold ${sortBy === 'bytes' ? 'bg-rose-955/40 text-rose-400' : 'bg-black text-zinc-500'}`}
                                        >
                                            BYTES
                                        </button>
                                        <button 
                                            id="cascade-sort-name-btn"
                                            name="sort_by_name"
                                            onClick={() => setSortBy('name')} 
                                            className={`py-1 font-bold ${sortBy === 'name' ? 'bg-rose-950/40 text-rose-400' : 'bg-black text-zinc-500'}`}
                                        >
                                            NAME
                                        </button>
                                    </div>
                                </div>

                                {/* Section type */}
                                <div className="flex flex-col gap-1">
                                    <span className="text-zinc-500 font-bold">CATEGORY</span>
                                    <div className="grid grid-cols-3 border border-zinc-850 rounded overflow-hidden">
                                        <button 
                                            id="cascade-category-all-btn"
                                            name="category_filter_all"
                                            onClick={() => setSelectedCategory('ALL')} 
                                            className={`py-1 font-bold ${selectedCategory === 'ALL' ? 'bg-rose-955/40 text-rose-400' : 'bg-black text-zinc-500'}`}
                                        >
                                            ALL
                                        </button>
                                        <button 
                                            id="cascade-category-comps-btn"
                                            name="category_filter_comps"
                                            onClick={() => setSelectedCategory('Component Module')} 
                                            className={`py-1 font-bold ${selectedCategory === 'Component Module' ? 'bg-rose-955/40 text-rose-400' : 'bg-black text-zinc-550'}`}
                                            title="React Components"
                                        >
                                            COMPS
                                        </button>
                                        <button 
                                            id="cascade-category-sys-btn"
                                            name="category_filter_sys"
                                            onClick={() => setSelectedCategory('System Core')} 
                                            className={`py-1 font-bold ${selectedCategory === 'System Core' ? 'bg-rose-955/40 text-rose-400' : 'bg-black text-zinc-500'}`}
                                            title="Core Engine Files"
                                        >
                                            SYS
                                        </button>
                                    </div>
                                </div>
                            </div>
                        </div>

                        {/* Module Cards Scroll Feed */}
                        <div className="space-y-2 max-h-[500px] overflow-y-auto pr-1 select-none custom-scrollbar">
                            {loading ? (
                                <div className="flex flex-col items-center justify-center py-16 bg-black/60 rounded-xl border border-zinc-900">
                                    <SpinnerIcon className="w-8 h-8 text-rose-500 animate-spin mb-3" />
                                    <span className="text-[10px] text-zinc-500 tracking-wider font-bold">CALCULATING CODE DENSITY VECTORS...</span>
                                </div>
                            ) : filteredModules.length === 0 ? (
                                <div className="text-center py-12 bg-black/60 border border-zinc-900 rounded-xl text-zinc-550 italic">
                                    No system modules found matching query metrics.
                                </div>
                            ) : (
                                <AnimatePresence>
                                    {filteredModules.map((m, idx) => {
                                        const isSelected = selectedModule?.path === m.path;
                                        const isBloated = m.lines > 600;
                                        const isOptimal = m.lines < 300;

                                        return (
                                            <motion.div
                                                key={m.path}
                                                id={`cascade-card-${m.name.replace(/\./g, '-')}`}
                                                initial={{ opacity: 0, y: 15 }}
                                                animate={{ opacity: 1, y: 0 }}
                                                transition={{ duration: 0.25, delay: Math.min(idx * 0.02, 0.45) }}
                                                onClick={() => setSelectedModule(m)}
                                                className={`group relative rounded-xl border-2 p-3 transition-all cursor-pointer ${
                                                    isSelected 
                                                        ? 'bg-rose-950/20 border-rose-500 shadow-[0_0_12px_rgba(244,63,94,0.15)] text-white' 
                                                        : 'bg-black/80 hover:bg-[#07070a] border-zinc-900 hover:border-zinc-805 text-zinc-300'
                                                }`}
                                            >
                                                {/* Top indicators */}
                                                <div className="flex justify-between items-start gap-1">
                                                    <div className="flex items-center gap-1.5 min-w-0">
                                                        <FileIcon className={`w-3.5 h-3.5 flex-shrink-0 ${isSelected ? 'text-rose-400' : 'text-zinc-550 group-hover:text-zinc-400'}`} />
                                                        <span className={`font-black tracking-tight truncate text-[11px] ${isSelected ? 'text-rose-400' : 'text-white'}`}>
                                                            {m.name}
                                                        </span>
                                                    </div>

                                                    <span className={`text-[8px] font-black px-1.5 py-0.5 rounded uppercase ${
                                                        m.category === 'System Core' 
                                                            ? 'bg-amber-955/40 text-amber-500 border border-amber-900/40' 
                                                            : 'bg-rose-955/40 text-rose-500 border border-rose-900/40'
                                                    }`}>
                                                        {m.category === 'System Core' ? 'SYSTEM' : 'COMP'}
                                                    </span>
                                                </div>

                                                {/* File stats list */}
                                                <div className="grid grid-cols-3 gap-2 mt-3 text-[9px] border-t border-zinc-950 pt-2 text-zinc-450 font-semibold">
                                                    <div className="flex flex-col">
                                                        <span className="text-[7.5px] text-zinc-600 uppercase">Lines</span>
                                                        <span className={`font-bold tabular-nums mt-0.5 ${isBloated ? 'text-amber-500' : (isOptimal ? 'text-emerald-400' : 'text-zinc-250')}`}>
                                                            {m.lines} {isBloated && '⚠️'}
                                                        </span>
                                                    </div>

                                                    <div className="flex flex-col">
                                                        <span className="text-[7.5px] text-zinc-600 uppercase">Bytes</span>
                                                        <span className="font-bold tabular-nums mt-0.5 text-zinc-200">
                                                            {formatBytes(m.bytes)}
                                                        </span>
                                                    </div>

                                                    <div className="flex flex-col mr-1">
                                                        <span className="text-[7.5px] text-zinc-650 uppercase">Blocks</span>
                                                        <span className="font-bold tabular-nums mt-0.5 text-cyan-400">
                                                            {m.functionsCount} fn
                                                        </span>
                                                    </div>
                                                </div>

                                                {/* Micro-meter background */}
                                                <div className="w-full bg-zinc-950 h-1 rounded overflow-hidden mt-2.5">
                                                    <div 
                                                        className={`h-full ${isBloated ? 'bg-amber-500' : (isOptimal ? 'bg-emerald-500' : 'bg-rose-500')}`}
                                                        style={{ width: `${Math.min((m.lines / 1000) * 100, 100)}%` }}
                                                    />
                                                </div>
                                            </motion.div>
                                        );
                                    })}
                                </AnimatePresence>
                            )}
                        </div>
                    </div>

                    {/* Right Side: Heavy Code block investigator & refactoring warnings */}
                    <div className="lg:col-span-12 xl:col-span-7 flex flex-col space-y-4">
                        {selectedModule ? (
                            <div className="bg-black/95 border-2 border-zinc-900 rounded-xl overflow-hidden shadow-[0_6px_24px_rgba(0,0,0,0.6)]">
                                {/* Selected Module Detail Banner */}
                                <div className="p-4 bg-[#050508] border-b border-zinc-900 flex justify-between items-center">
                                    <div>
                                        <div className="flex items-center gap-2">
                                            <FileIcon className="w-4 h-4 text-rose-500" />
                                            <h2 className="text-[12px] font-black text-rose-400 uppercase tracking-wider">{selectedModule.name}</h2>
                                        </div>
                                        <div className="text-[8.5px] text-zinc-500 mt-1 uppercase">
                                            PATH: <span className="text-zinc-350">{selectedModule.path}</span>
                                        </div>
                                    </div>
                                    <button 
                                        id="cascade-close-inspect-btn"
                                        name="close_inspector"
                                        onClick={() => setSelectedModule(null)}
                                        className="p-1 rounded-lg border border-zinc-805 hover:border-zinc-700 hover:bg-zinc-900 transition-colors"
                                    >
                                        <XIcon className="w-3.5 h-3.5 text-zinc-400" />
                                    </button>
                                </div>

                                {/* Detail Tab Selector */}
                                <div className="flex border-b border-zinc-900 bg-[#040406] px-4 pt-2 gap-1 overflow-x-auto scrollbar-none">
                                    <button 
                                        id="details-tab-audit"
                                        onClick={() => { setDetailsTab('audit'); playTapSound(500, 'sine', 0.05); }}
                                        className={`pb-2 px-3 text-[9px] border-b-2 font-black uppercase transition-all tracking-wider cursor-pointer flex-shrink-0 ${detailsTab === 'audit' ? 'border-rose-500 text-rose-400 font-bold' : 'border-transparent text-zinc-500 hover:text-zinc-300'}`}
                                    >
                                        [ HEALTH AUDIT ]
                                    </button>
                                    <button 
                                        id="details-tab-dependencies"
                                        onClick={() => { setDetailsTab('dependencies'); playTapSound(550, 'sine', 0.05); }}
                                        className={`pb-2 px-3 text-[9px] border-b-2 font-black uppercase transition-all tracking-wider cursor-pointer flex-shrink-0 ${detailsTab === 'dependencies' ? 'border-rose-500 text-rose-400' : 'border-transparent text-zinc-500 hover:text-zinc-300 font-bold'}`}
                                    >
                                        [ DEPENDENCY TOPOLOGY ]
                                    </button>
                                    <button 
                                        id="details-tab-refactor"
                                        onClick={() => { setDetailsTab('refactor'); playTapSound(600, 'sine', 0.05); }}
                                        className={`pb-2 px-3 text-[9px] border-b-2 font-black uppercase transition-all tracking-wider cursor-pointer flex-shrink-0 ${detailsTab === 'refactor' ? 'border-rose-500 text-rose-400 font-bold' : 'border-transparent text-zinc-500 hover:text-zinc-300'}`}
                                    >
                                        [ DECOMPOSITION SANDBOX ]
                                    </button>
                                    <button 
                                        id="details-tab-source"
                                        onClick={() => { setDetailsTab('source'); playTapSound(650, 'sine', 0.05); }}
                                        className={`pb-2 px-3 text-[9px] border-b-2 font-black uppercase transition-all tracking-wider cursor-pointer flex-shrink-0 ${detailsTab === 'source' ? 'border-rose-500 text-rose-400 font-bold' : 'border-transparent text-zinc-500 hover:text-zinc-300'}`}
                                    >
                                        [ SOURCE DETECTOR ]
                                    </button>
                                </div>

                                <div className="p-4 bg-black/60">
                                    {detailsTab === 'audit' && (
                                        <div className="space-y-4 animate-fade-in">
                                            {/* Health & Maintainability Index Banner */}
                                            <div className="bg-[#040406] border border-zinc-900 rounded-xl p-4 flex items-center justify-between">
                                                {(() => {
                                                    const score = selectedModule.maintainabilityIndex || Math.max(15, Math.min(100, Math.round(100 - (selectedModule.lines / 10))));
                                                    const scoreColor = score >= 80 ? 'text-emerald-400' : score >= 50 ? 'text-amber-500' : 'text-rose-500';
                                                    const strokeDashoffset = 113 - (113 * score) / 100;
                                                    
                                                    return (
                                                        <div className="flex items-center gap-4">
                                                            <div className="relative w-12 h-12 flex-shrink-0">
                                                                <svg className="w-full h-full transform -rotate-90">
                                                                    <circle cx="24" cy="24" r="18" stroke="#121214" strokeWidth="3.5" fill="transparent" />
                                                                    <motion.circle 
                                                                        cx="24" cy="24" r="18" 
                                                                        stroke={score >= 80 ? '#34d399' : score >= 50 ? '#f59e0b' : '#ef4444'} 
                                                                        strokeWidth="3.5" 
                                                                        fill="transparent" 
                                                                        strokeDasharray="113"
                                                                        initial={{ strokeDashoffset: 113 }}
                                                                        animate={{ strokeDashoffset }}
                                                                        transition={{ duration: 1, ease: "easeOut" }}
                                                                    />
                                                                </svg>
                                                                <div className="absolute inset-0 flex items-center justify-center text-[10px] font-black">
                                                                    {score}%
                                                                </div>
                                                            </div>
                                                            <div>
                                                                <span className="text-[10px] font-black uppercase text-zinc-300 block">Maintainability score</span>
                                                                <span className={`text-[8px] font-black uppercase ${scoreColor}`}>
                                                                    {score >= 80 ? 'HEALTHY MODULAR MODULE' : score >= 50 ? 'MEDIUM SPLIT WARNING' : 'HEAVY DESTRUCTURING ADVISED'}
                                                                </span>
                                                            </div>
                                                        </div>
                                                    );
                                                })()}
                                                
                                                <div className="text-right text-[8px] text-zinc-500">
                                                    DENSITY RATIO: <span className="text-zinc-350">{(selectedModule.chars / selectedModule.lines).toFixed(1)} chars/ln</span>
                                                </div>
                                            </div>

                                            {/* Bento Grid Metrics */}
                                            <div className="grid grid-cols-1 md:grid-cols-3 gap-3">
                                                <div className="bg-zinc-950/50 border border-zinc-900 rounded-xl p-3 flex flex-col justify-between">
                                                    <span className="text-[8px] text-zinc-500 uppercase font-black">Memory density</span>
                                                    <div className="text-sm font-black text-white mt-1">{(selectedModule.chars / selectedModule.lines).toFixed(1)} <span className="text-[9px] text-zinc-650 font-normal">chars/ln</span></div>
                                                    <span className="text-[7.5px] text-zinc-600 font-bold mt-1 uppercase">Syntax density ratio</span>
                                                </div>

                                                <div className="bg-zinc-950/50 border border-zinc-900 rounded-xl p-3 flex flex-col justify-between">
                                                    <span className="text-[8px] text-zinc-500 uppercase font-black">Composition complexity</span>
                                                    <div className="text-sm font-black text-rose-400 mt-1">
                                                        {selectedModule.importsCount} <span className="text-[9px] text-zinc-650">imports</span>
                                                    </div>
                                                    <span className="text-[7.5px] text-zinc-600 font-bold mt-1 uppercase">Import dependencies</span>
                                                </div>

                                                <div className="bg-zinc-950/50 border border-zinc-900 rounded-xl p-3 flex flex-col justify-between">
                                                    <span className="text-[8px] text-zinc-500 uppercase font-black">React Hook density</span>
                                                    <div className="text-sm font-black text-cyan-400 mt-1">
                                                        {selectedModule.hooksCount} <span className="text-[9px] text-zinc-600 font-normal font-bold">registered</span>
                                                    </div>
                                                    <span className="text-[7.5px] text-zinc-650 font-bold mt-1 uppercase">Core hooks used</span>
                                                </div>
                                            </div>

                                            {/* Breakdowns of React Hooks usage */}
                                            <div className="bg-[#030305] border border-zinc-900 rounded-xl p-3 grid grid-cols-2 gap-4 text-[9px]">
                                                <div>
                                                    <span className="text-zinc-550 block font-bold uppercase mb-1">State Hooks used</span>
                                                    <span className="text-zinc-300 font-bold">{selectedModule.useStateCount ?? 0} x useState() variables detected</span>
                                                </div>
                                                <div>
                                                    <span className="text-zinc-550 block font-bold uppercase mb-1">Side effects triggered</span>
                                                    <span className="text-zinc-300 font-bold">{selectedModule.useEffectCount ?? 0} x useEffect() declarations</span>
                                                </div>
                                            </div>

                                            {/* Refactoring logic advice */}
                                            {selectedModule.lines > 600 ? (
                                                <div className="p-3 bg-amber-950/15 border border-amber-500/20 text-yellow-500 rounded-xl flex items-start gap-2.5">
                                                    <WarningIcon className="w-4 h-4 mt-0.5 flex-shrink-0 animate-pulse text-amber-400" />
                                                    <div className="text-[9px]">
                                                        <div className="font-bold uppercase text-[9.5px] text-amber-400">ARCHITECTURAL DEFACTOR WARNING</div>
                                                        <div className="text-zinc-400 mt-0.5 leading-relaxed">
                                                            This module contains over <span className="text-amber-400 font-black">{selectedModule.lines} lines of code</span>. We advise decomposing large UI/system blocks into sub-components in the <span className="text-zinc-200">components/</span> directory to prevent token buffer saturation and facilitate faster browser DOM cascading. Use the <strong className="text-zinc-200">Decomposition Sandbox</strong> tab to preview a virtual split.
                                                        </div>
                                                    </div>
                                                </div>
                                            ) : (
                                                <div className="p-3 bg-emerald-950/10 border border-emerald-500/20 text-emerald-400 rounded-xl flex items-start gap-2.5">
                                                    <CheckCircleIcon className="w-4 h-4 mt-0.5 flex-shrink-0 text-emerald-400" />
                                                    <div className="text-[9px]">
                                                        <div className="font-bold uppercase text-[9.5px]">OPTIMAL BLOCK ALLOCATION</div>
                                                        <div className="text-zinc-400 mt-0.5 leading-relaxed">
                                                            This file sits comfortably within safe compiler limits (<span className="text-emerald-400 font-bold">{selectedModule.lines} lines</span>). No immediate modular splits are required for physical maintainability.
                                                        </div>
                                                    </div>
                                                </div>
                                            )}
                                        </div>
                                    )}

                                    {detailsTab === 'dependencies' && (
                                        <div className="space-y-4 animate-fade-in">
                                            {/* Dependency Topology Graph */}
                                            <div className="relative h-64 bg-[#020204] border border-zinc-900 rounded-xl p-4 overflow-hidden flex flex-col justify-between">
                                                <div className="absolute inset-0 bg-[linear-gradient(rgba(255,255,255,0.015)_1px,transparent_1px),linear-gradient(90deg,rgba(255,255,255,0.015)_1px,transparent_1px)] bg-[size:15px_15px] pointer-events-none" />
                                                
                                                {(() => {
                                                    const rawDeps = selectedModule.allImports || [];
                                                    const uniqueDeps = Array.from(new Set(rawDeps.length > 0 ? rawDeps : ['react', 'motion/react', './icons', 'recharts']));
                                                    
                                                    return (
                                                        <div className="relative w-full h-full flex items-center justify-center">
                                                            {/* SVG connecting vectors */}
                                                            <svg className="absolute inset-0 w-full h-full pointer-events-none" xmlns="http://www.w3.org/2000/svg">
                                                                {uniqueDeps.map((dep, idx) => {
                                                                    const angle = (idx * 2 * Math.PI) / uniqueDeps.length;
                                                                    const radius = 75;
                                                                    // center is roughly 165, 105 in current layout spacing
                                                                    const cx = 165 + Math.cos(angle) * radius;
                                                                    const cy = 105 + Math.sin(angle) * radius;
                                                                    
                                                                    return (
                                                                        <motion.line
                                                                            key={idx}
                                                                            x1="165"
                                                                            y1="105"
                                                                            x2={cx}
                                                                            y2={cy}
                                                                            stroke="rgba(244, 63, 94, 0.25)"
                                                                            strokeWidth="1.5"
                                                                            strokeDasharray="4,4"
                                                                            initial={{ pathLength: 0 }}
                                                                            animate={{ pathLength: 1 }}
                                                                            transition={{ duration: 0.6, delay: idx * 0.08 }}
                                                                        />
                                                                    );
                                                                })}
                                                            </svg>

                                                            {/* Center node */}
                                                            <motion.div 
                                                                animate={{ scale: [1, 1.05, 1] }}
                                                                transition={{ repeat: Infinity, duration: 4 }}
                                                                className="absolute z-10 w-20 h-20 rounded-full border-2 border-rose-500 bg-rose-950/40 flex flex-col items-center justify-center text-center shadow-[0_0_15px_rgba(239,68,68,0.2)] p-1 cursor-default"
                                                                style={{ left: '125px', top: '65px' }}
                                                            >
                                                                <span className="text-[8px] font-black text-rose-400 truncate w-full">{selectedModule.name}</span>
                                                                <span className="text-[6.5px] text-zinc-500 uppercase mt-0.5">CORE MODULE</span>
                                                            </motion.div>

                                                            {/* Orbiting dependency nodes */}
                                                            {uniqueDeps.map((dep, idx) => {
                                                                const angle = (idx * 2 * Math.PI) / uniqueDeps.length;
                                                                const radius = 75;
                                                                // Calculate coordinates radiating from the center (165, 105)
                                                                const x = 165 + Math.cos(angle) * radius - 45; // center offset minus half width of node
                                                                const y = 105 + Math.sin(angle) * radius - 14; // center offset minus half height of node
                                                                const isThirdParty = !dep.startsWith('.') && !dep.startsWith('/');

                                                                return (
                                                                    <motion.button
                                                                        key={dep}
                                                                        id={`dep-node-${idx}`}
                                                                        initial={{ opacity: 0, scale: 0 }}
                                                                        animate={{ opacity: 1, scale: 1 }}
                                                                        transition={{ type: 'spring', stiffness: 120, delay: idx * 0.06 }}
                                                                        onClick={() => playTapSound(300 + idx * 80, 'triangle', 0.06)}
                                                                        className={`absolute z-20 px-2 py-1 rounded-lg border text-[7.5px] font-bold truncate w-[90px] shadow-lg transition-all cursor-pointer ${
                                                                            isThirdParty 
                                                                                ? 'bg-cyan-950/30 border-cyan-500 text-cyan-400 hover:bg-cyan-950/60' 
                                                                                : 'bg-emerald-950/30 border-emerald-500 text-emerald-400 hover:bg-emerald-950/60'
                                                                        }`}
                                                                        style={{ left: `${x}px`, top: `${y}px` }}
                                                                        title={dep}
                                                                    >
                                                                        {dep.split('/').pop()}
                                                                    </motion.button>
                                                                );
                                                            })}
                                                        </div>
                                                    );
                                                })()}

                                                <div className="relative z-10 flex justify-between items-center text-[8px] text-zinc-550 border-t border-zinc-900 pt-2">
                                                    <span className="flex items-center gap-2">
                                                        <span className="inline-block w-2 h-2 rounded-sm bg-cyan-500" /> Third-party package
                                                        <span className="inline-block w-2 h-2 rounded-sm bg-emerald-500 ml-1" /> Internal helper
                                                    </span>
                                                    <span>DIAGRAM SCHEMATIC SECURE</span>
                                                </div>
                                            </div>

                                            {/* Dynamic Imports Detail Listing */}
                                            <div className="bg-[#040406] border border-zinc-900 rounded-xl p-3 space-y-2">
                                                <span className="text-[8px] text-zinc-550 uppercase font-black block border-b border-zinc-950 pb-1.5">// IMPORT BLOCK METADATA</span>
                                                <div className="grid grid-cols-1 md:grid-cols-2 gap-2 max-h-24 overflow-y-auto custom-scrollbar pr-1">
                                                    {selectedModule.imports.map((imp, idx) => (
                                                        <div key={idx} className="bg-black/45 border border-zinc-900/60 rounded px-2 py-1 flex items-center justify-between text-[8px]">
                                                            <span className="text-zinc-300 truncate w-4/5">{imp}</span>
                                                            <span className="text-rose-500 text-[7px] font-mono">OK</span>
                                                        </div>
                                                    ))}
                                                </div>
                                            </div>
                                        </div>
                                    )}

                                    {detailsTab === 'refactor' && (
                                        <div className="space-y-4 animate-fade-in">
                                            {/* Virtual Decomposition Sandbox Container */}
                                            <div className="grid grid-cols-1 md:grid-cols-12 gap-4 items-start">
                                                {/* Left column: Metrics preview */}
                                                <div className="md:col-span-4 bg-[#040406] border border-zinc-900 rounded-xl p-3.5 space-y-3">
                                                    <span className="text-[8.5px] text-zinc-550 font-black uppercase tracking-widest block border-b border-zinc-950 pb-1">// SPLIT METRICS</span>
                                                    
                                                    <div className="space-y-2.5">
                                                        <div className="flex justify-between items-center text-[9px]">
                                                            <span className="text-zinc-500 font-semibold">ORIGINAL LEVEL:</span>
                                                            <span className="text-rose-400 font-bold tabular-nums">
                                                                {selectedModule.maintainabilityIndex || Math.max(15, Math.min(100, Math.round(100 - (selectedModule.lines / 10))))}%
                                                            </span>
                                                        </div>
                                                        <div className="flex justify-between items-center text-[9px]">
                                                            <span className="text-emerald-400 font-bold">OPTIMAL SPLIT LEVEL:</span>
                                                            <span className="text-emerald-400 font-black tabular-nums">96%</span>
                                                        </div>
                                                        <div className="flex justify-between items-center text-[9px]">
                                                            <span className="text-zinc-500 font-semibold">MAIN FILE SAVINGS:</span>
                                                            <span className="text-amber-500 font-bold tabular-nums">~65% reduction</span>
                                                        </div>
                                                        <div className="flex justify-between items-center text-[9px]">
                                                            <span className="text-zinc-500 font-semibold">SEGMENTS:</span>
                                                            <span className="text-cyan-400 font-bold">3 Modular files</span>
                                                        </div>
                                                    </div>

                                                    <div className="pt-2 border-t border-zinc-950">
                                                        <span className="text-[7px] text-zinc-650 leading-relaxed uppercase block">
                                                            // DECONSTRUCTING MULTI-LAYER COMPOSITION INTO BALANCED SEGMENTS STABILIZES BROWSER RUNTIMES
                                                        </span>
                                                    </div>
                                                </div>

                                                {/* Right column: Sandbox file switcher & mock code */}
                                                <div className="md:col-span-8 flex flex-col space-y-2">
                                                    <div className="flex border border-zinc-900 rounded-lg overflow-hidden text-[8px] bg-black">
                                                        <button 
                                                            onClick={() => { setSimulatedSplitPart('types'); playTapSound(400, 'sine', 0.05); }}
                                                            className={`flex-1 py-1.5 font-bold uppercase ${simulatedSplitPart === 'types' ? 'bg-rose-950/40 text-rose-400' : 'text-zinc-500'}`}
                                                        >
                                                            Types file
                                                        </button>
                                                        <button 
                                                            onClick={() => { setSimulatedSplitPart('logic'); playTapSound(480, 'sine', 0.05); }}
                                                            className={`flex-1 py-1.5 font-bold uppercase ${simulatedSplitPart === 'logic' ? 'bg-rose-950/40 text-rose-400' : 'text-zinc-500'}`}
                                                        >
                                                            Logic Hook
                                                        </button>
                                                        <button 
                                                            onClick={() => { setSimulatedSplitPart('view'); playTapSound(560, 'sine', 0.05); }}
                                                            className={`flex-1 py-1.5 font-bold uppercase ${simulatedSplitPart === 'view' ? 'bg-rose-950/40 text-rose-400' : 'text-zinc-500'}`}
                                                        >
                                                            View Component
                                                        </button>
                                                    </div>

                                                    {/* Code sandbox block */}
                                                    {(() => {
                                                        const splits = generateVirtualSplitFiles(selectedModule, moduleContent);
                                                        const currentCode = simulatedSplitPart === 'types' ? splits.typesCode : simulatedSplitPart === 'logic' ? splits.logicCode : splits.viewCode;
                                                        
                                                        return (
                                                            <div className="bg-[#020204] border border-zinc-900 rounded-xl p-3 max-h-48 overflow-auto custom-scrollbar font-mono text-[9px] relative leading-normal">
                                                                <pre className="text-zinc-300">
                                                                    <code>{currentCode}</code>
                                                                </pre>
                                                            </div>
                                                        );
                                                    })()}
                                                </div>
                                            </div>

                                            <div className="text-[8px] text-zinc-650 italic text-center">
                                                Note: Decompositions are run inside memory simulations and will not mutate real host files.
                                            </div>
                                        </div>
                                    )}

                                    {detailsTab === 'source' && (
                                        <div className="space-y-2 animate-fade-in">
                                            <div className="flex justify-between items-center text-[9px] text-zinc-550 border-b border-zinc-900 pb-2">
                                                <span className="font-black uppercase tracking-widest text-zinc-400 flex items-center gap-1.5">
                                                    <TerminalIcon className="w-3.5 h-3.5 text-rose-500" />
                                                    LIVE COMPILER CODE DETECTOR
                                                </span>
                                                <span>
                                                    {loadingContent ? 'LOADING BYTES...' : `${selectedModule.chars} CHARACTERS LOADED`}
                                                </span>
                                            </div>

                                            <div className="bg-[#020204] border border-zinc-900 rounded-xl p-3 max-h-[350px] overflow-auto custom-scrollbar font-mono text-[10px] leading-relaxed relative">
                                                {loadingContent ? (
                                                    <div className="flex flex-col items-center justify-center py-20 text-zinc-500">
                                                        <SpinnerIcon className="w-6 h-6 text-rose-500 animate-spin mb-2" />
                                                        <span>Siphoning source blocks from host...</span>
                                                    </div>
                                                ) : (
                                                    <pre className="text-zinc-300 selection:bg-rose-500/40 tab-size">
                                                        <code>
                                                            {moduleContent.split('\n').map((line, idx) => (
                                                                <div key={idx} className="hover:bg-zinc-900/30 px-1 py-0.5 flex">
                                                                    <span className="w-10 text-zinc-650 text-right select-none pr-3 flex-shrink-0">{idx + 1}</span>
                                                                    <span className={line.trim().startsWith('import') ? 'text-rose-400' : line.trim().startsWith('export') ? 'text-amber-400' : 'text-zinc-300'}>
                                                                        {line || ' '}
                                                                    </span>
                                                                </div>
                                                            ))}
                                                        </code>
                                                    </pre>
                                                )}
                                            </div>

                                            <div className="flex justify-between items-center text-[8.5px] text-zinc-600 uppercase pt-2">
                                                <span>// STRETCH RATIO: OK</span>
                                                <span>ENCODING: UTF-8 SECURE //</span>
                                            </div>
                                        </div>
                                    )}
                                </div>
                            </div>
                        ) : (
                            <div className="bg-black/60 border-2 border-zinc-905 border-dashed rounded-xl py-32 text-center text-zinc-500 flex flex-col items-center justify-center space-y-3 px-6 shadow-inner">
                                <GaugeIcon className="w-8 h-8 text-zinc-650 animate-bounce" />
                                <div className="space-y-1">
                                    <div className="text-[10px] font-bold text-zinc-400 uppercase tracking-widest">Select a Module on the Left</div>
                                    <div className="text-[8.5px] text-zinc-600 max-w-sm">
                                        Activate diagnostics tracing on any file module or core block inside the repository tree to analyze lines, byte weights, imports, functional layout, and trigger live cascades.
                                    </div>
                                </div>
                            </div>
                        )}
                    </div>
                </div>
            ) : (
                <motion.div 
                    animate={{ x: shakingResonance ? [-4, 4, -4, 4, -1, 1, 0] : 0 }} 
                    transition={{ duration: 0.25 }}
                    className="grid grid-cols-1 lg:grid-cols-12 gap-6 items-start animate-fade-in"
                >
                    {/* Left Side: AAPT Class Raw Editor & Live Grid Node Selector */}
                    <div className="lg:col-span-5 flex flex-col space-y-4">
                        
                        {/* AAPT Editor Console */}
                        <div className="bg-black border-2 border-zinc-900 rounded-xl p-4 space-y-3 shadow-2xl relative overflow-hidden">
                            <div className="absolute top-0 right-0 w-24 h-24 bg-cyan-400/5 rounded-full blur-2xl pointer-events-none" />
                            <div className="flex justify-between items-center border-b border-zinc-900 pb-2">
                                <div className="flex items-center gap-2">
                                    <TerminalIcon className="w-4 h-4 text-cyan-400 animate-pulse" />
                                    <span className="text-[10px] font-black text-cyan-400 uppercase tracking-widest">AAPT SOURCE CODE INJECTOR</span>
                                </div>
                                <span className="text-[8px] text-zinc-500 uppercase">// PACKAGE: github.com.Rkwideman42</span>
                            </div>

                            <div className="space-y-1">
                                <label className="text-[8px] text-zinc-500 uppercase font-bold">// EDIT RAW R.java INPUT TO RE-COMPILE ADDRESSES</label>
                                <textarea
                                    id="r-java-editor-textarea"
                                    value={rJavaInput}
                                    onChange={(e) => {
                                        setRJavaInput(e.target.value);
                                        setSimulatedAaptLog(`[AAPT COMPILE SIGNAL] Parsed ${aaptResources.length} definitions on user modification.`);
                                        playTapSound(440, 'triangle', 0.03);
                                    }}
                                    className="w-full h-44 bg-[#020204] text-zinc-300 font-mono text-[9px] p-2 rounded-lg border border-zinc-900 focus:outline-none focus:border-cyan-500 focus:ring-1 focus:ring-cyan-500/20 resize-none leading-normal"
                                    placeholder="Paste class R.java here..."
                                />
                            </div>

                            {/* Simulated Compile Console Log */}
                            <div className="bg-[#050508] border border-zinc-900 rounded-lg p-2 font-mono text-[8px] text-emerald-400 flex items-center justify-between">
                                <div className="truncate flex items-center gap-1.5">
                                    <span className="text-zinc-500 font-bold select-none">&gt;&gt;</span>
                                    <span>{simulatedAaptLog}</span>
                                </div>
                                <button
                                    id="aapt-compile-reset-btn"
                                    onClick={() => {
                                        setRJavaInput(INITIAL_R_JAVA);
                                        setSimulatedAaptLog('[AAPT RESTORE] Reset back to standard Rkwideman42 template definitions.');
                                        playTapSound(600, 'sine', 0.05);
                                    }}
                                    className="px-2 py-0.5 border border-zinc-800 hover:border-cyan-500 hover:bg-cyan-950/20 text-zinc-400 hover:text-cyan-400 rounded transition-colors text-[8px] font-bold"
                                >
                                    RESET
                                </button>
                            </div>
                        </div>

                        {/* Interactive Ingest List Finder */}
                        <div className="bg-black border-2 border-zinc-900 rounded-xl p-4 space-y-4 shadow-sm">
                            <div className="flex justify-between items-center border-b border-zinc-900 pb-2">
                                <span className="text-[10px] font-black text-cyan-400 uppercase tracking-widest">INGESTED AAPT CHANNELS</span>
                                <span className="text-[8px] text-zinc-500 font-semibold">{filteredAaptResources.length} items parsed</span>
                            </div>

                            {/* Inline search filter */}
                            <div className="relative">
                                <input 
                                    id="aapt-res-search"
                                    type="text"
                                    placeholder="Filter by variable name (e.g. abc_fade)..."
                                    value={searchAaptQuery}
                                    onChange={(e) => setSearchAaptQuery(e.target.value)}
                                    className="w-full bg-[#050508] border border-zinc-800 rounded-xl px-3 py-1.5 pl-8 text-white focus:outline-none focus:border-cyan-500 text-[10px]"
                                />
                                <SearchIcon className="w-3.5 h-3.5 text-zinc-500 absolute left-2.5 top-2" />
                            </div>

                            {/* Class Category Pills */}
                            <div className="flex flex-wrap gap-1.5">
                                <button
                                    id="aapt-filter-pill-all"
                                    onClick={() => { setSelectedAaptClass('all'); playTapSound(500, 'sine', 0.04); }}
                                    className={`px-2 py-1 rounded text-[9px] font-black uppercase transition-all cursor-pointer ${selectedAaptClass === 'all' ? 'bg-cyan-950/40 border border-cyan-500 text-cyan-400 shadow-[0_0_8px_rgba(6,182,212,0.15)] font-bold' : 'bg-black border border-zinc-900 text-zinc-500 hover:text-zinc-300'}`}
                                >
                                    ALL
                                </button>
                                {aaptClasses.map(clsName => (
                                    <button
                                        key={clsName}
                                        id={`aapt-filter-pill-${clsName}`}
                                        onClick={() => { setSelectedAaptClass(clsName); playTapSound(700, 'sine', 0.04); }}
                                        className={`px-2 py-1 rounded text-[9px] font-black uppercase transition-all cursor-pointer ${selectedAaptClass === clsName ? 'bg-cyan-950/40 border border-cyan-500 text-cyan-400 font-bold' : 'bg-black border border-zinc-900 text-zinc-500 hover:text-zinc-300'}`}
                                    >
                                        [ {clsName} ]
                                    </button>
                                ))}
                            </div>

                            {/* Extracted nodes flex grid */}
                            <div className="grid grid-cols-2 gap-1.5 max-h-[220px] overflow-y-auto pr-1 custom-scrollbar">
                                {filteredAaptResources.map((res) => {
                                    const isSource = compareSourceId === res.name;
                                    const isTarget = compareTargetId === res.name;
                                    const isFocusSelected = selectedAaptRes?.name === res.name;

                                    return (
                                        <div
                                            key={res.name}
                                            id={`aapt-res-node-${res.name}`}
                                            onClick={() => {
                                                setSelectedAaptRes(res);
                                                if (!compareSourceId || (compareSourceId && compareTargetId && isTarget)) {
                                                    setCompareSourceId(res.name);
                                                    playTapSound(600, 'sine', 0.04);
                                                } else {
                                                    setCompareTargetId(res.name);
                                                    playTapSound(750, 'sine', 0.05);
                                                }
                                            }}
                                            className={`p-2 rounded-lg border text-left cursor-pointer transition-all ${
                                                isSource 
                                                    ? 'bg-rose-950/15 border-rose-500 text-rose-300 font-black' 
                                                    : isTarget 
                                                        ? 'bg-cyan-955 border-cyan-500 text-cyan-300 font-black'
                                                        : isFocusSelected
                                                            ? 'bg-amber-955 border-amber-500 text-amber-305'
                                                            : 'bg-[#030305] border-zinc-900 hover:border-zinc-800 text-zinc-400 hover:text-zinc-200'
                                            }`}
                                        >
                                            <div className="flex justify-between items-center">
                                                <span className="text-[7.5px] text-zinc-500 uppercase font-black tracking-wider">{res.className}</span>
                                                {isSource && <span className="text-[7px] text-rose-500 font-black px-1 build-tag bg-rose-950/40 border border-rose-900">[SRC]</span>}
                                                {isTarget && <span className="text-[7px] text-cyan-500 font-black px-1 build-tag bg-cyan-955 border border-cyan-900">[TGT]</span>}
                                            </div>
                                            <div className="text-[10px] font-bold mt-1 truncate">{res.name}</div>
                                            <div className="text-[9px] text-zinc-500 uppercase mt-0.5 font-mono tracking-wide tabular-nums">{res.hex}</div>
                                        </div>
                                    );
                                })}
                            </div>

                            <div className="text-[8px] text-zinc-650 italic leading-relaxed">
                                Tip: Click any card on the listing to load it as Red (Source) or Cyan (Target) respectively to visualize their exact physical block connections.
                            </div>
                        </div>
                    </div>

                    {/* Right Side: Visual Cascade Difference Canvas outliner & Willpower system */}
                    <div className="lg:col-span-12 xl:col-span-7 flex flex-col space-y-4">
                        
                        {/* THE GRAPHICAL CASCADE DIFFERENCE ENGINE */}
                        <div className="bg-black border-2 border-zinc-900 rounded-xl p-4 shadow-xl space-y-4 relative overflow-hidden">
                            <div className="absolute top-0 right-0 w-44 h-44 bg-cyan-500/5 rounded-full blur-3xl pointer-events-none" />
                            <div className="flex justify-between items-center border-b border-zinc-900 pb-2">
                                <div className="flex items-center gap-2">
                                    <ActivityIcon className="text-cyan-400 w-4 h-4 animate-pulse" />
                                    <h3 className="text-[11px] font-black tracking-widest text-cyan-400 uppercase">DRAWING OUTLINE TO CASCADING DIFFERENCE</h3>
                                </div>
                                <span className="text-[8.5px] bg-cyan-950/50 text-cyan-400 border border-cyan-900/40 px-2 py-0.5 rounded uppercase font-bold font-mono">OUTLINE GENERATOR v1.02</span>
                            </div>

                            {/* The Interactive Drawing Stage */}
                            <div className="relative h-48 bg-[#020204] border border-zinc-900 rounded-xl p-3 flex flex-col justify-between overflow-hidden">
                                
                                {/* Absolute positioning grid lines */}
                                <div className="absolute inset-0 bg-[linear-gradient(rgba(255,255,255,0.015)_1px,transparent_1px),linear-gradient(90deg,rgba(255,255,255,0.015)_1px,transparent_1px)] bg-[size:10px_10px] pointer-events-none" />

                                {addrDiffValue ? (
                                    <>
                                        {/* Cumulative step-stair outlines representing physical cascading delta distances */}
                                        <div className="absolute inset-x-8 inset-y-12 flex items-center justify-between pointer-events-none z-10">
                                            {/* Source Node */}
                                            <motion.div 
                                                animate={{ scale: [1, 1.15, 1] }} 
                                                transition={{ repeat: Infinity, duration: 2 }}
                                                className="w-10 h-10 rounded-full border-2 border-rose-500 bg-rose-950/40 flex flex-col items-center justify-center relative shadow-[0_0_12px_rgba(239,68,68,0.2)]"
                                            >
                                                <span className="text-[7.5px] font-black text-rose-400 uppercase">SRC</span>
                                                <span className="text-[6.5px] text-zinc-400 font-mono tabular-nums">{addrDiffValue.sourceRes.hex.substring(6)}</span>
                                                
                                                <div className="absolute -bottom-5 left-0 right-0 text-center text-[7px] font-bold text-rose-400 truncate w-16 -ml-3">
                                                    {addrDiffValue.sourceRes.name}
                                                </div>
                                            </motion.div>

                                            {/* Connecting stairway paths representing physical delta distances */}
                                            <div className="flex-1 h-20 mx-4 relative flex items-center justify-center">
                                                <svg className="absolute inset-0 w-full h-full" xmlns="http://www.w3.org/2000/svg">
                                                    <defs>
                                                        <linearGradient id="neonGradient" x1="0%" y1="0%" x2="100%" y2="100%">
                                                            <stop offset="0%" stopColor="#ef4444" />
                                                            <stop offset="100%" stopColor="#06b6d4" />
                                                        </linearGradient>
                                                    </defs>
                                                    
                                                    {/* Draw step outlines dynamically dependent on the size of delta gap */}
                                                    <motion.path
                                                        d={`M 10,40 L ${30 + outlinePadding * 5},40 L ${30 + outlinePadding * 5},${60 - outlinePadding * 3} L 110,${60 - outlinePadding * 3}`}
                                                        fill="none"
                                                        stroke="url(#neonGradient)"
                                                        strokeWidth={outlinePadding}
                                                        strokeDasharray="5,3"
                                                        initial={{ strokeDashoffset: 100 }}
                                                        animate={{ strokeDashoffset: [100, 0] }}
                                                        transition={{ repeat: Infinity, duration: 4 / (1 + willPower / 100), ease: 'linear' }}
                                                    />

                                                    {/* Outer orbit cycle vectors activated on willPower increases */}
                                                    {rotationOutline && (
                                                        <motion.circle
                                                            cx="60"
                                                            cy="50"
                                                            r={25 + willPower / 5}
                                                            fill="none"
                                                            stroke="rgba(6, 182, 212, 0.15)"
                                                            strokeWidth="1"
                                                            animate={{ rotate: 360 }}
                                                            transition={{ repeat: Infinity, duration: 15 / (1 + willPower / 100), ease: 'linear' }}
                                                            style={{ transformOrigin: '60px 50px' }}
                                                        />
                                                    )}
                                                </svg>

                                                {/* Visual staircase labels overlay */}
                                                <div className="absolute inset-x-0 top-1/2 -translate-y-1/2 flex justify-between px-2 text-[7px] text-zinc-650 tracking-tighter">
                                                    <span>STEP_0x00</span>
                                                    <span>DIFFERENCE OFFSET OUTLINE</span>
                                                    <span className="text-cyan-400">DIFF_DEC:{addrDiffValue.diffDecimal}</span>
                                                </div>
                                            </div>

                                            {/* Target Node */}
                                            <motion.div 
                                                animate={{ scale: [1, 1.15, 1] }} 
                                                transition={{ repeat: Infinity, duration: 2, delay: 0.5 }}
                                                className="w-10 h-10 rounded-full border-2 border-cyan-500 bg-cyan-950/40 flex flex-col items-center justify-center relative shadow-[0_0_12px_rgba(6,182,212,0.2)]"
                                            >
                                                <span className="text-[7.5px] font-black text-cyan-400 uppercase font-black">TGT</span>
                                                <span className="text-[6.5px] text-zinc-400 font-mono tabular-nums">{addrDiffValue.targetRes.hex.substring(6)}</span>

                                                <div className="absolute -bottom-5 left-0 right-0 text-center text-[7px] font-bold text-cyan-400 truncate w-16 -ml-3">
                                                    {addrDiffValue.targetRes.name}
                                                </div>
                                            </motion.div>
                                        </div>

                                        <div className="relative z-10 p-2.5 flex justify-between bg-black/60 border border-zinc-900 rounded-lg">
                                            <div className="flex flex-col">
                                                <span className="text-[7.5px] text-zinc-550 uppercase font-black tracking-widest">DELTA SUBTRACTION OFFSET</span>
                                                <span className="text-sm font-black text-white mt-1 tabular-nums">
                                                    {addrDiffValue.sourceRes.hex} - {addrDiffValue.targetRes.hex} = <span className="text-cyan-400 font-black">{addrDiffValue.diffHex}</span>
                                                </span>
                                            </div>
                                            <div className="text-right flex flex-col justify-between">
                                                <span className="text-[7.5px] text-zinc-550 uppercase font-black">SPACING RELATION</span>
                                                <span className="text-[9.5px] text-cyan-400 font-bold mt-1">
                                                    {addrDiffValue.relation} ({addrDiffValue.diffDecimal} steps)
                                                </span>
                                            </div>
                                        </div>

                                        <div className="relative z-10 flex justify-between text-[8px] text-zinc-550 uppercase">
                                            <span>// CONNECTOR RADIUS WIDTH: {outlinePadding}px</span>
                                            <span>STATUS: OUTLINE GRAPHED SECURELY</span>
                                        </div>
                                    </>
                                ) : (
                                    <div className="m-auto text-center text-zinc-600 font-bold text-[10px] uppercase">
                                        Select at least two distinct variables on left to calculate cascade outlines!
                                    </div>
                                )}
                            </div>

                            {/* Canvas Visual Outline Config Adjustments */}
                            <div className="grid grid-cols-2 gap-3">
                                <div className="bg-[#040406] border border-zinc-900 p-2 rounded-xl flex items-center justify-between">
                                    <span className="text-[8.5px] text-zinc-500 font-bold uppercase">OUTLINE stroke width:</span>
                                    <div className="flex items-center gap-1.5">
                                        <input
                                            id="outline-padding-range"
                                            type="range"
                                            min="1"
                                            max="8"
                                            value={outlinePadding}
                                            onChange={(e) => {
                                                setOutlinePadding(parseInt(e.target.value));
                                                playTapSound(350, 'sine', 0.05);
                                            }}
                                            className="w-16 accent-cyan-500 h-1 cursor-pointer"
                                        />
                                        <span className="text-[8px] font-mono text-cyan-400 tabular-nums w-4 text-center">{outlinePadding}px</span>
                                    </div>
                                </div>

                                <div className="bg-[#040406] border border-zinc-900 p-2 rounded-xl flex items-center justify-between">
                                    <span className="text-[8.5px] text-zinc-500 font-bold uppercase">ANGULAR CYCLING VECTORS:</span>
                                    <button
                                        id="rotation-outline-toggle"
                                        onClick={() => {
                                            setRotationOutline(!rotationOutline);
                                            playTapSound(480, 'sine', 0.04);
                                        }}
                                        className={`px-3 py-1 rounded text-[8px] font-black uppercase transition-all cursor-pointer ${rotationOutline ? 'bg-cyan-955 border border-cyan-500 text-cyan-400': 'bg-black border border-zinc-900 text-zinc-600'}`}
                                    >
                                        {rotationOutline ? 'ENABLED' : 'DISABLED'}
                                    </button>
                                </div>
                            </div>
                        </div>

                        {/* WILLPOWER GENERATOR MODULE */}
                        <div className="bg-black border-2 border-zinc-900 rounded-xl p-4 shadow-xl space-y-4">
                            <div className="flex justify-between items-center border-b border-zinc-900 pb-2">
                                <div className="flex items-center gap-2">
                                    <FireIcon className="text-amber-500 w-4 h-4" />
                                    <span className="text-[11px] font-black tracking-widest text-[#f59e0b] uppercase">WILLPOWER GENERATOR ENGINE</span>
                                </div>
                                <span className="text-[8px] text-zinc-550 mr-1">// RESONANCE COUPLER ACTIVATED</span>
                            </div>

                            <div className="bg-zinc-950 border border-zinc-900 p-3 rounded-xl space-y-3">
                                <div className="flex justify-between items-center text-[10px] text-zinc-450 font-semibold">
                                    <span className="flex items-center gap-1 text-zinc-300">
                                        <ZapIcon className="w-3.5 h-3.5 text-amber-400 animate-bounce" />
                                        AMPLIFY COGNITIVE WILLPOWER
                                    </span>
                                    <span className={`font-black tabular-nums ${willPower > 80 ? 'text-amber-400 font-bold animate-pulse' : 'text-zinc-200'}`}>{willPower}% FOCUS</span>
                                </div>

                                <div className="flex items-center gap-3">
                                    <span className="text-[8.5px] text-zinc-550 select-none">LOW</span>
                                    <input 
                                        id="willpower-slide-bar"
                                        type="range"
                                        min="0"
                                        max="100"
                                        step="5"
                                        value={willPower}
                                        onChange={(e) => {
                                            const val = parseInt(e.target.value);
                                            setWillPower(val);
                                            playTapSound(220 + val * 6, 'triangle', 0.05);
                                        }}
                                        className="flex-1 accent-amber-500 cursor-pointer h-1.5"
                                    />
                                    <span className="text-[8.5px] text-amber-500 font-black">OVERLOAD</span>
                                </div>

                                <div className="grid grid-cols-5 gap-1 pt-1 select-none">
                                    {Array.from({ length: 5 }).map((_, i) => {
                                        const isActive = willPower >= (i + 1) * 20;
                                        return (
                                            <div 
                                                key={i} 
                                                className={`h-1.5 rounded transition-colors ${isActive ? (willPower > 80 ? 'bg-amber-500 animate-pulse' : 'bg-cyan-500') : 'bg-zinc-900'}`}
                                            />
                                        );
                                    })}
                                </div>
                            </div>

                            {/* Focus Release Trigger Block */}
                            <div className="flex flex-col space-y-2">
                                <button
                                    id="ignite-willpower-burst-btn"
                                    onClick={() => {
                                        setResonanceShaking(true);
                                        setSimulatedAaptLog(`[WILLPOWER CHIME IGNITED] Pulsed ${willPower}% focus at Delta offset ${addrDiffValue?.diffHex || '0x00'} successfully.`);
                                        playTapSound(400, 'sawtooth', 0.12);
                                        setTimeout(() => playTapSound(800, 'sine', 0.1), 120);
                                        setTimeout(() => playTapSound(1600, 'sine', 0.2), 240);
                                        setTimeout(() => {
                                            setResonanceShaking(false);
                                        }, 400);
                                    }}
                                    className="w-full relative group overflow-hidden py-3 bg-gradient-to-r from-rose-600 to-amber-500 hover:from-rose-500 hover:to-amber-450 text-white font-black uppercase text-xs rounded-xl shadow-lg shadow-red-500/10 hover:shadow-red-500/25 transition-all text-center tracking-widest cursor-pointer"
                                >
                                    <span className="relative z-10 flex items-center justify-center gap-2">
                                        <FireIcon className="w-4 h-4 animate-bounce text-yellow-300" />
                                        [ IGNITE CANONICAL WILLPOWER BURST ]
                                    </span>
                                </button>
                                <span className="text-[7.5px] text-zinc-650 text-center select-none uppercase">// IGNITING WILLPOWER BURST OVERLOADS CONNECTOR MATRIX TO TRIGGER SUBTLE STAGE RESONANCE</span>
                            </div>
                        </div>

                    </div>
                </motion.div>
            )}
            
            {/* Design Footer Information block */}
            <div className="mt-8 border-t border-zinc-900 pt-4 flex flex-col sm:flex-row justify-between items-center text-[8.5px] text-zinc-550 select-none">
                <span>SYSTEM DIAGNOSTIC ENGINE v5.24 // AETHER-SENSING SUITE ACTIVATED</span>
                <span className="mt-2 sm:mt-0 font-bold">BYPASSING COLD ENGINE THROTTLE // CHRIST ACTIVE SECURE LAYER</span>
            </div>
        </div>
    );
};
